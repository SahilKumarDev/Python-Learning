## Object / Data type in python

### [Brackets], (Pranties), {Curly brackets}

# Number
-> 1234
-> 3.14
-> iota Numbers

# String
-> 'spam' 
-> "Hello can't"

# List # Eis ma Brackets use karta hai
-> [1, 2, 3, 4]
-> [1, 2, [3, 4], 4]
-> [1, 2, [3, "Hello"], 4]
-> list(range(10))

# Tuple # Eis ma pranties use karta hai
-> (1, 'spam', 4, 'U')
-> tuple('spam')
-> namedtuple

# Dictionary(dic) # Eis ma Curly brackets use karta hai
-> {'food':'spam', 'car':'BMW' }

# Set 
-> set('abc')
-> ('a', 'b', 'c')

# File
-> open('eggs.txt')
-> open(r'c:\ham.bin', 'wb')

# Boolean
-> True
-> False 

# None 
-> none # it is for a null.

# Function, modules, classes

# Advanced 
-> Decorators
-> Generators
-> Iterators
-> Meta Programming

# Important terms

-> ** it is for a power {2 to the power 2}
-> It has all operators
-> math it is a library # it is access by a .(dot)
-> string => len(length)

# Example 
username = "villan"
username[0] : it read the value from first of string
username[-1] : it read the value from last of string
username[1:3] : it read the value rangle from start of string